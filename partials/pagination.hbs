<nav class="pagination" role="navigation">
    {{#if prev}}
        <a class="newer-posts" href="{{page_url prev}}"><span class="fa fa-chevron-left"></span> Neuere Posts</a>
    {{/if}}
    <span class="page-number">Seite {{page}} von {{pages}}</span>
    {{#if next}}
        <a class="older-posts" href="{{page_url next}}">Ältere Posts <span class="fa fa-chevron-right"></span></a>
    {{/if}}
</nav>
